def ms_to_seconds(ms: int) -> float:
    """Converts time from milliseconds to seconds.

    Args:
        num (int): Time in milliseconds

    Returns:
        float: Time in seconds.
    """
    return ms / 1000
